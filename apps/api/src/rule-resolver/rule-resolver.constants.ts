import { RULE_LAYERS } from '@hrm/shared-types';

/** Resolution order — ARCHITECTURE.md §3 */
export const RULE_LAYER_ORDER = RULE_LAYERS;

export const RULE_SOURCE = Symbol('RULE_SOURCE');
export const EMPLOYEE_CONTEXT = Symbol('EMPLOYEE_CONTEXT');
